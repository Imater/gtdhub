---
- name: Apply common configuration to all nodes
  hosts:
    - gtdhub-node
#    - 4tree-build
#    - 4tree-db
  vars_files:
    - 'defaults/main.yml'
    - 'defaults/production.yml'
  user: root
  roles:
#   - role: rabbitmq
    - role: common
    - role: nginx
    - role: nodejs
#    - role: mariadb
    - role: supervisor
    - role: rsnapshot

